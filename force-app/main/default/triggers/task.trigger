trigger task on Task (before insert,After Insert,before update) {
    /*if(trigger.isInsert){
        if(trigger.isBefore){
            taskTriggerHandler.updatePriority(trigger.new);
        }
        if(trigger.isAfter){
            taskTriggerHandler.updatetheContact(trigger.new);
        }
    }
    if(trigger.isBefore && trigger.isUpdate){
        taskTriggerHandler.updatethecheckbox(trigger.new);
    }
    */
}